package Crawlerfj.Common;

public class Const {
    public enum elementHandleAction{
        download,
        getTag,
    }

    public enum taskType{
        getHtmlElement,
        getContent,
        redirect
    }
}
